runConfig:
  cpu: 1
  memoryMiB: 1024
  maxInstances: 2
  minInstances: 0
  concurrency: 100

# Using the latest stable build of the Google Cloud Next.js buildpack
# https://github.com/GoogleCloudPlatform/buildpacks/releases
build:
  builder: gcr.io/buildpacks/builder:v1